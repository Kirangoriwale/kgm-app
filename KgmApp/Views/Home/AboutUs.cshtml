@model KgmApp.Models.AboutUsContent
@{
    var canEdit = ViewMenu.Can(ViewData, MenuKeys.AboutUs, "Edit");
    var existingHtml = (Model.ContentHtml ?? string.Empty).Trim();
    var hasContent = !string.IsNullOrWhiteSpace(existingHtml)
        && !string.Equals(existingHtml, "<p><br></p>", StringComparison.OrdinalIgnoreCase);
}

@section Head {
    <link href="https://cdn.jsdelivr.net/npm/quill@1.3.7/dist/quill.snow.css" rel="stylesheet" />
}

<div class="card border-0 shadow-sm rounded-3">
    <div class="card-body p-3 p-md-4">
        @if (canEdit)
        {
            <form asp-action="SaveAboutUs" method="post" id="aboutUsForm">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Id" />
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                <label asp-for="ContentHtml" class="form-label fw-semibold">About Us Content</label>
                <div id="aboutUsEditor" class="meeting-minutes-editor"></div>
                <textarea asp-for="ContentHtml" class="d-none"></textarea>
                <span asp-validation-for="ContentHtml" class="text-danger"></span>

                <div class="mt-3 d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary">Save About Us</button>
                </div>
            </form>
        }
        else if (!hasContent)
        {
            <div class="text-muted">About Us content is not added yet.</div>
        }
        else
        {
            <div class="small text-body">@Html.Raw(existingHtml)</div>
        }
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    @if (canEdit)
    {
        <script src="https://cdn.jsdelivr.net/npm/quill@1.3.7/dist/quill.min.js"></script>
        <script>
            (function () {
                const form = document.getElementById('aboutUsForm');
                const hiddenInput = document.getElementById('ContentHtml');
                const editorHost = document.getElementById('aboutUsEditor');
                if (!form || !hiddenInput || !editorHost || typeof Quill === 'undefined') return;

                const quill = new Quill(editorHost, {
                    theme: 'snow',
                    placeholder: 'Add About Us content...',
                    modules: {
                        toolbar: [
                            [{ 'header': [1, 2, false] }],
                            ['bold', 'italic', 'underline'],
                            [{ list: 'ordered' }, { list: 'bullet' }],
                            ['link', 'clean']
                        ]
                    }
                });

                if (hiddenInput.value) {
                    quill.clipboard.dangerouslyPasteHTML(hiddenInput.value);
                }

                form.addEventListener('submit', function () {
                    const html = quill.root.innerHTML;
                    hiddenInput.value = html === '<p><br></p>' ? '' : html;
                });
            })();
        </script>
    }
}
